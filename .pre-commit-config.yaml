repos:
  - repo: local
    hooks:
      - id: flake8
        name: flake8
        entry: flake8 scripts tests
        language: python
        additional_dependencies:
          - flake8==7.1.1
        files: ^(scripts|tests)/.*\.py$
        pass_filenames: false
